# Boilerplate-Django

```
# initial a local git repo
source git_init.sh 
```
```
# create virtual python env , install required packages and start a Django project at this folder.
# if your os is mac , adjust `python` command in install.sh 
source install.sh 
```
